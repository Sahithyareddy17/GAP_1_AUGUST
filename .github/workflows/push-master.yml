name: Push to master only
 
on:
  push:
    branches: [ master ]   # ✅ runs only when you push to master
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
 
      - name: Show trigger
        run: echo "🚀 This runs ONLY on master branch"
 
      - name: Build step
        run: echo "🔨 Building project..."
 
  deploy:
    runs-on: ubuntu-latest
    needs: build   # ✅ runs only after build succeeds
    steps:
      - name: Deploy step
        run: echo "🚢 Deploying project (master branch only)..."
     
